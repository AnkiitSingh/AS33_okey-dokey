import React from "react";
import { withRouter } from "react-router-dom";

const Nav = ({ history }) => {
    return (
        <div>
            <h1>Hello there</h1>
        </div>
    )
}

export default withRouter(Nav);